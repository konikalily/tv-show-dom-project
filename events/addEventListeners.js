import handleEpisodeSelect from "./handleEpisodeSelect.js";
import handleSelectReset from "./handleSelectReset.js";
import handleSearch from "./handleSearch.js";
import handleShowSelect from "./handleShowSelect.js";

export default function addEventListeners(allEpisodesArray) {
; //when user provides input to search episodes

  // element.addEventListener("eventName", callback) - if only the event is passed to callback
  // element.addEventListener("eventName", () => callback(event, ...more parameters)) - if more than event is needed


}
